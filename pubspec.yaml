name: dictionary_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  http: ^1.6.0
  provider: ^6.1.5+1
  
  # --- THÊM 2 THƯ VIỆN NÀY ĐỂ DÙNG CHỨC NĂNG LOA ---
  audioplayers: ^5.2.1
  flutter_tts: ^4.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/mock_dictionary.json
    - assets/mock_thesaurus.json